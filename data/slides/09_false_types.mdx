import cayleyF2 from '/assets/cayley_f2.svg'

# Those are the ends
<div style={{ textAlign: 'right', fontSize: 12, color: 'grey' }}>
*beautiful friends \\ those are the ends \\ my only friends, the ends*
</div>
<br />

We say that a subgraph $P\subseteq T$ is a **ray** if it is isometric with $[0,\infty)$.
<br />
<div style={{ textAlign: 'center' }}>
$P\sim P'$ iff $P\cap P'$ is an infinite subgraph
</div>
<br />

<Theorem title="ends of a tree">
  The set of equivalence classes of rays in a tree is called its **boundary**, $\partial T$.
</Theorem>
<br />

### Examples

- $\mathbb{R}_+$ as a tree has one boundary component

<svg 
  width="400" 
  height="60" 
  viewBox="0 0 400 60" 
  style={{ 
    display: 'block', 
    margin: 'auto', 
    overflow: 'visible' /* Ensures the trailing dots show up */
  }}
>
  {/* The Line: Starts at center of first dot (40) and ends at 340 */}
  <line x1="40" y1="30" x2="340" y2="30" stroke="currentColor" strokeWidth="2" />
  
  {/* The Nodes (TikZ style) */}
  {[40, 100, 160, 220, 280].map(x => (
    <circle key={x} cx={x} cy="30" r="4" fill="currentColor" />
  ))}
  
  {/* The Three Dots (Ellipsis) - Using circles for reliability */}
  <g fill="currentColor">
    <circle cx="355" cy="30" r="2" />
    <circle cx="367" cy="30" r="2" />
    <circle cx="379" cy="30" r="2" />
  </g>
</svg>

- $\mathbb{R}$ as an infinite tree has two boundary components

<svg 
  width="500" 
  height="60" 
  viewBox="0 0 500 60" 
  style={{ 
    display: 'block', 
    margin: 'auto', 
    overflow: 'visible' 
  }}
>
  {/* The Main Line: Extending from the first ellipsis to the last */}
  <line x1="60" y1="30" x2="440" y2="30" stroke="currentColor" strokeWidth="2" />
  
  {/* The Nodes (TikZ style) - Centered around the middle */}
  {[100, 150, 200, 250, 300, 350, 400].map(x => (
    <circle key={x} cx={x} cy="30" r="4" fill="currentColor" />
  ))}
  
  {/* Left Ellipsis (Negative Infinity) */}
  <g fill="currentColor">
    <circle cx="25" cy="30" r="2" />
    <circle cx="37" cy="30" r="2" />
    <circle cx="49" cy="30" r="2" />
  </g>

  {/* Right Ellipsis (Positive Infinity) */}
  <g fill="currentColor">
    <circle cx="451" cy="30" r="2" />
    <circle cx="463" cy="30" r="2" />
    <circle cx="475" cy="30" r="2" />
  </g>
</svg>

- Cayley graph of $F_2$ has infinitely many boundary components (it do be $\{0,1\}^\mathbb{N}$)

<img src={cayleyF2} alt="Cayley grpah of the free group with two generators" style={{ width: '20%', maxWidth: '800px', margin: 'auto', display: 'block' }} />

There are no other options.
