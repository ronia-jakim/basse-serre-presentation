export const Item = ({ name, active, children }) => (
  <span style={{ 
    color: active === name ? '#5F9C3F' : '#888888', 
    fontWeight: active === name ? 'bold' : 'normal',
    transition: 'all 0.3s ease'
  }}>
    { children }
  </span>
)

### Basse-Serre theory

**Tree isometries**

- <Item name="minsets" active={props.active}>minsets</Item>
- <Item name="types" active={props.active}>linear, parabolic, hyperbolic</Item>

**Structure from action**

- <Item name="fundamental domain" active={props.active}>simple fundamental domain</Item>
- <Item name="connected" active={props.active}>connected grpah</Item>
- <Item name="no cycles" active={props.active}>no cycles</Item>

**Simple examples**

- <Item name="dihedral" active={props.active}>$D_\infty$</Item>
- <Item name="SL(2, Z)" active={props.active}>$SL(2,\mathbb{Z})$</Item>

**Basse-Serre tree**

- <Item name="graph of groups" active={props.active}>graph of groups</Item>
- <Item name="limit" active={props.active}>limit of a graph of groups</Item>
- <Item name="topology" active={props.active}>topolooogy? - fundamental group etc</Item>
