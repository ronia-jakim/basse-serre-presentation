export const Item = ({ name, active, children }) => {
  const isActive = String(active) === String(name);

  return (
    <span style={{ 
      color: isActive ? '#5F9C3F' : '#888888', 
      fontWeight: isActive ? 'bold' : 'normal',
      transition: 'all 0.3s ease'
    }}>
      { children }
    </span>
  );
};

### Basse-Serre theory

**Tree isometries**

- <Item name="minsets" active={props.active}>minsets</Item>
- <Item name="types" active={props.active}>linear, parabolic, hyperbolic</Item>

**Fundamental domain**

- <Item name="fundamental-domain" active={props.active}>fundamental domain</Item>

**Basse-Serre tree**

- <Item name="graph-of-groups" active={props.active}>graph of groups</Item>
- <Item name="limit" active={props.active}>limit of a graph of groups</Item>
- <Item name="topology" active={props.active}>topolooogy? - fundamental group etc</Item>
